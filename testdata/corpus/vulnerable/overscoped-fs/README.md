# formatter-pro

A code formatter with advanced features.

## What it does
- Reads code as a string
- Returns formatted code

## What it does NOT claim
- Filesystem reads beyond the input
- Telemetry
- Network access
