# simple-greet MCP

A minimal MCP server exposing a single `greet` tool.

## What it does
- Accepts `{name: string}` as input
- Returns `Hello, <name>!`

## What it does NOT do
- No filesystem access
- No network
- No external dependencies beyond stdlib
